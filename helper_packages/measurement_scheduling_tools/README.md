# measurement-scheduling-tools
Functions to help determine when to make measurements. Some of this would be obvious or simple with Unix timestamps, but these can introduce error-prone time-zone double-conversion steps.

Currently, includes `datetime_range`. See example notebooks for more information.

